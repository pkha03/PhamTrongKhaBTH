@model IEnumerable<BTVN2003.Models.Student>
    <p>
        <a  class="btn btn-success" asp-action="Create">Create a new Student</a>
    </p>
    <table class="table table-all">
        <tr>
            <th>StudentID</th>
            <th>StudentName</th>
            
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(m => item.StudentID)</td>
                <td>@Html.DisplayFor(m => item.StudentName)</td>
            </tr>
            <td>
                <a asp-action ="Edit" asp-route-id="@item.StudentID">Success</a>
                <a asp-action ="Delete" asp-route-id="@item.StudentID">Delete</a>
            </td>
        }
       


    </table>